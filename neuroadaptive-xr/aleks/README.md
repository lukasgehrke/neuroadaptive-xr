# Simulation

1. Run `demo.py` (this sets a random seed, to get the same output every time)
2. For a fancy live plot, run `log_plot_live.ipynb`

Make sure to Control+C `demo.py` after it's done.

Sometimes LSL streams don't start or cannot be found. A restart of the terminal/
machine can help.